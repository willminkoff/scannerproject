# 2026-06-12 CPU mitigation: pool sized to favorite (airband=9ch, ground=20ch)
# + headroom. Default 32/64 built full-rate FIR chains for every unclaimed
# slot (~600-800 GR threads, load 70+, mounts at 30%% of real-time).
# Permanent fix is Phase 3 topology park savings.
[Service]
Environment="CHIRP_MAX_CHANNELS=12"
