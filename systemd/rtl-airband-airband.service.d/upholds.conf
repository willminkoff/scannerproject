[Unit]
# Keep rtl-airband-ground active whenever rtl-airband-airband is active.
# Fixes cascade-restart bug: airband-ui's gentle restart of airband
# triggers a Requires-cascade stop of ground; this restores it.
Upholds=rtl-airband-ground.service
