<icecast>
  <location>SprontPi</location>
  <admin>admin@localhost</admin>

  <limits>
    <clients>100</clients>
    <sources>10</sources>
    <threadpool>5</threadpool>
    <queue-size>16384</queue-size>
    <client-timeout>30</client-timeout>
    <header-timeout>15</header-timeout>
    <source-timeout>1</source-timeout>
    <burst-size>4096</burst-size>
  </limits>

  <authentication>
    <source-password>062352</source-password>
    <relay-password>062352</relay-password>
    <admin-user>source</admin-user>
    <admin-password>062352</admin-password>
  </authentication>

  <listen-socket>
    <port>8000</port>
  </listen-socket>

  <!-- Airband source (rtl_airband) -->
  <mount>
    <mount-name>/AIR.mp3</mount-name>
    <type>audio/mpeg</type>
    <fallback-mount>/keepalive.mp3</fallback-mount>
    <fallback-override>1</fallback-override>
  </mount>

  <!-- Ground source (SDRTrunk) -->
  <mount>
    <mount-name>/GROUND.mp3</mount-name>
    <type>audio/mpeg</type>
    <fallback-mount>/keepalive.mp3</fallback-mount>
    <fallback-override>1</fallback-override>
  </mount>

  <!-- Mixed output -->
  <mount>
    <mount-name>/GND.mp3</mount-name>
    <type>audio/mpeg</type>
    <fallback-mount>/keepalive.mp3</fallback-mount>
    <fallback-override>1</fallback-override>
  </mount>

  <!-- Staging mount for DMR testing -->
  <mount>
    <mount-name>/DMR.mp3</mount-name>
    <type>audio/mpeg</type>
    <fallback-mount>/keepalive.mp3</fallback-mount>
    <fallback-override>1</fallback-override>
  </mount>

  <paths>
    <basedir>/usr/share/icecast2</basedir>
    <logdir>/var/log/icecast2</logdir>
    <webroot>/usr/share/icecast2/web</webroot>
    <adminroot>/usr/share/icecast2/admin</adminroot>
    <alias source="/" destination="/status.xsl"/>
  </paths>

  <logging>
    <accesslog>access.log</accesslog>
    <errorlog>error.log</errorlog>
    <loglevel>3</loglevel>
    <logsize>10000</logsize>
  </logging>

  <security>
    <chroot>0</chroot>
  </security>
</icecast>
