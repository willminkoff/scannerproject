# Drop-in: enable audio-path tracing on ground (mirrors airband).
#
# See chirp/systemd/gr-demod@airband.service.d/zz-audio-trace.conf for
# field meanings.  Probe with:
#   python3 scripts/chirp-audio-path-probe.py --port 7401 --interval 0.5

[Service]
Environment="CHIRP_AUDIO_TRACE=1"
